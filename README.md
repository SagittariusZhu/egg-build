# egg-build
Package eggjs app

**Install**
```
npm install egg-build --save-dev
yarn add egg-build -D
```

Modify your package.json, add line
```
  "scripts": {
    "build": "egg-build",
    ...
  },

```

Then, you can use
```
npm run build
yarn build
```